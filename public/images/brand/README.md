# Marca — Flávio Milhomem

**Arquivo em uso:** `logo-monogram.png` — monograma do design (PNG transparente, 558×394).

Tamanhos no site: CSS `.fm-header-logo-wordmark` e `.fm-footer-logo` (inalterados).

Config: `src/config/brand.ts` · componente: `BrandLogo`.

**Legado:** `elementos_marca-13.jpg` — versão anterior.

**Hero aluno (banner):** `elementos_marca-18.png` — arquivo do manual de marca, sem alteração de pixels. A animação revela esse asset via máscara SVG (`npm run trace:bridge` só gera paths de máscara a partir do arquivo; não redesenha a arte).
