# Marca — Flávio Milhomem

**Arquivo em uso:** `elementos_marca-13.jpg` — cópia fiel do arquivo enviado pelo cliente (1024×1024).

**Sem edição no repo:** sem recorte, sem remover fundo branco, sem recolor. O site só redimensiona via CSS/`next/image`.

Para trocar pela logo corrigida: substituir `elementos_marca-13.jpg` (mesmo nome ou atualizar `src/config/brand.ts`).

Config: `src/config/brand.ts` · componente: `BrandLogo`.

**Hero aluno (banner):** `elementos_marca-18.png` — arquivo do manual de marca, sem alteração de pixels. A animação revela esse asset via máscara SVG (`npm run trace:bridge` só gera paths de máscara a partir do arquivo; não redesenha a arte).
