import { LoadingSpinner } from "@/components/shared/loading-spinner";

/** Ver a nota em `loading-spinner.tsx`: este arquivo NÃO pode subir para a raiz. */
export default function Loading() {
  return <LoadingSpinner />;
}
